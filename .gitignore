# ==========================================================================
# .gitignore —— 给 Git 的一份「这些文件别管」清单
# --------------------------------------------------------------------------
# 它只被 Git 读取，**不影响 Skill 运行**：脚本、模板、判定逻辑完全不受影响。
# 唯一作用：拦住下面列出的路径，不让它们被 git add 上传到 GitHub。
#
# 为什么必须拦：本 Skill 产出的 results/ final/ out/、答题卡扫描件、台账 xlsx
# 里都带着学生的**真实姓名、学号、成绩和手写笔迹**。这些一旦推到公开仓库，
# 就等于把学生个人信息公开了 —— 而且**删掉文件也删不掉 Git 历史**，
# 只能重写历史或删库重建。所以宁可在源头拦住。
#
# 如果你把它删掉：Skill 照样能跑，但从此以后 git add -A 会把上面那些文件
# 一起收进去（详见 README「数据与隐私」一节）。
# ==========================================================================

# ---- 学生数据：绝对不要提交 ----
# 答题卡扫描件、批改报告、任何含学号/姓名的路径
部分答题卡/
部分报告/
答题卡/
*/部分答题卡/
*/部分报告/
*.jpg
*.jpeg
*.png

# 批改过程与产物（含学生姓名与成绩）
results/
results_bak/
final/
out/
*.xlsx
*.pdf

# 手动上传前由 pack_for_upload.py 生成的打包目录
上传到GitHub/
upload_pack/

# ---- 付费改造（Pay Skill）的敏感面：绝对不要提交 ----
# 商家应用私钥、支付宝公钥、serviceId、appId 等本地配置
skillpay.local.json
*.pem
*.key
*.p8
# 订单库（含 out_trade_no / trade_no / 支付状态）与联调产物
state/
*.db
*.db-wal
*.db-shm

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
